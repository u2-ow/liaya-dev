---
import Layout from "../layouts/Layout.astro";
import { Header } from "../components/Header";
import { MV } from "../components/Mv";
import { ArticlePostArea } from "../components/ArticlePostArea";
import { getBlogs } from "../lib/microcms";
const blogs = await getBlogs({ fields: ["title", "id", "content"] });
console.log(blogs);
---

<Layout title="Welcome to Astro.">
  <Header />
  <MV />
  <ArticlePostArea blog={blogs} />
</Layout>

<style></style>
